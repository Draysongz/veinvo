{
  "name": "turborepo-basic",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "packageManager": "npm@10.7.0",
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
